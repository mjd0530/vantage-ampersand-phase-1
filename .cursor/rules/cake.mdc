---
description: cake& design system — how to build UI in this prototype
alwaysApply: true
---

Read [AGENTS.md](../../AGENTS.md) before writing any UI, and
`context/cake-components.md` before using any cake& component.

Never hardcode a color, spacing, radius, or type size — use the `var(--…)`
tokens listed in `context/cake-tokens.md`. Never add a second UI library.
