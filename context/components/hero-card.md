<!-- GENERATED by scripts/build-agent-context.mjs. Do not edit. -->
# HeroCard

```ts
import { HeroCard } from '@cake-admin/cakeand';
```

The marketing hero card template (Figma hero card, node 183:11829, and its centered banner card variant, node 183:11878). It's content that slots into a Card: an optional leading eyebrow (icon + text), a large display title, one or two body paragraphs, and an actions row.

## Props

| Prop | Type | Default | Notes |
| --- | --- | --- | --- |
| `leadingText` | `React.ReactNode` | — | Eyebrow text above the title (Figma "Leading text"). |
| `leadingIcon` | `React.ReactNode` | — | Optional 24px icon before the leading text; inherits the text color. |
| `title` | `React.ReactNode` | **required** | The display title (Figma 42px bold.42). |
| `body` | `React.ReactNode` | — | Primary body paragraph (Figma 20px regular.20.title). |
| `secondaryBody` | `React.ReactNode` | — | Secondary body paragraph (Figma 16px regular.16). |
| `actions` | `React.ReactNode` | — | Actions row — compose a cake& Button (fill for hero, tonal for banner). |
| `align` | `'start' \| 'center'` | `'start'` | start is the left-aligned hero (Figma hero card); center is the centered banner (Figma banner card). |

Also accepts all `Omit<React.HTMLAttributes<HTMLDivElement>, 'title'>` props.

## Usage

```tsx
import { Card, HeroCard } from '@/cakeand/components/Card';
import { Button } from '@/cakeand/components/Button';

// hero (left, fill button)
<Card>
  <HeroCard
    leadingIcon={<BadgeCheck />}
    leadingText="Leading text"
    title="Hero card title"
    body="Primary paragraph…"
    secondaryBody="Secondary paragraph…"
    actions={<Button size="lg" endIcon={<ArrowRight />}>Label</Button>}
  />
</Card>

// banner (centered, tonal button)
<Card>
  <HeroCard
    align="center"
    title="Hero card title"
    body="…"
    actions={<Button size="lg" variant="tonal" endIcon={<ArrowRight />}>Launch</Button>}
  />
</Card>
```

## Design tokens used

`--space-600`, `--space-300`, `--space-100`, `--type-size-subject`, `--font-weight-regular`, `--color-text-icon-primary`, `--type-size-hero`, `--font-weight-bold`, `--type-size-title`

## Accessibility

- The title renders as an <h2> — keep the document's heading order sane around it (a hero is usually the top of its section).
- leadingIcon is decorative (aria-hidden); the leading text carries meaning.
- Put a real Button (or link) in actions; it brings its own accessible name and focus ring.

## Do / Don't

| Do | Don't |
| --- | --- |
| Use align="center" for the banner treatment | Center-align long multi-paragraph copy — it's hard to read |
| Pair the hero with a fill Button, the banner with tonal | Stack several hero cards on one page — one hero per view |
| Slot it into a <Card> | Render it bare — it has no surface or elevation of its own |
| Keep the title short and declarative | Put a paragraph in the 42px title slot |

---

- Behaviour comes from a Radix primitive; cake& owns the visuals.
- Related: Button, Card.
- Source: `src/cakeand/components/Card/`
- Storybook: <https://cake.lenovo.com/storybook/?path=/docs/components-card-hero-card--docs>
