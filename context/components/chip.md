<!-- GENERATED by scripts/build-agent-context.mjs. Do not edit. -->
# Chip

```ts
import { Chip } from '@cake-admin/cakeand';
```

Chip represents a compact, discrete piece of information — a filter, a selection, or a removable input token. Choose a type to reflect the chip's meaning, add a leading icon for recognition, and provide onRemove to make the chip dismissable. Use Badge for non-interactive status text and Button for primary actions.

## Props

| Prop | Type | Default | Notes |
| --- | --- | --- | --- |
| `children` | `React.ReactNode` | **required** | Chip text. |
| `type` | `'primary' \| 'secondary' \| 'success' \| 'warn' \| 'error' \| 'info'` | `'primary'` | Semantic color family from the Figma type variant. Drives the overlay background wash. |
| `size` | `'sm' \| 'lg'` | `'lg'` | Figma size: lg is a 32px / 14px chip, sm is a 24px / 12px chip. |
| `disabled` | `boolean` | `false` | Dims the chip and disables the body/remove interactions (Figma state=disabled). |
| `leadingIcon` | `React.ReactNode` | — | Optional leading glyph slot; inherits color via currentColor. |
| `onClick` | `React.MouseEventHandler<HTMLButtonElement>` | — | Fires when the chip body is activated. When provided the body becomes a real <button>; omit it for a display-only chip. |
| `onRemove` | `React.MouseEventHandler<HTMLButtonElement>` | — | Fires when the trailing remove control is activated. When provided a remove button (Radix AccessibleIcon) renders in the trailing slot. |
| `removeLabel` | `string` | — | Accessible name for the remove control. Defaults to Remove <label> when the children are a plain string, otherwise Remove. |

Also accepts all `Omit<React.HTMLAttributes<HTMLSpanElement>, 'onClick'>` props.

## Usage

```tsx
<Chip>Label</Chip>
<Chip type="success" leadingIcon={<CheckCircle2 />}>Verified</Chip>
<Chip onRemove={() => remove(id)}>Removable</Chip>
<Chip onClick={() => toggle()}>Selectable</Chip>
<Chip size="sm" type="info">Compact</Chip>
<Chip disabled onRemove={fn()}>Disabled</Chip>
```

## Design tokens used

`--color-primary-primary-overlay`, `--color-primary-primary-overlay-hover`, `--color-secondary-secondary-overlay`, `--color-secondary-secondary-overlay-hover`, `--color-success-success-overlay`, `--color-success-success-overlay-hover`, `--color-warning-warn-overlay`, `--color-warning-warn-overlay-hover`, `--color-error-error-overlay`, `--color-error-error-overlay-hover`, `--color-info-info-overlay`, `--color-info-info-overlay-hover`, `--color-text-icon-primary`, `--font-weight-bold`, `--type-size-body`, `--type-size-caption`, `--radius-1000`, `--space-100`, `--space-050`, `--stroke-300`, `--color-primary-primary`, `--color-disabled-disabled`, `--color-disabled-disabled-inverse`

## Accessibility

- The remove control is a real <button> whose icon-only glyph is named through Radix AccessibleIcon; customize it with removeLabel.
- A clickable body (onClick) renders as a <button>; a display-only chip renders no interactive element.
- The focus ring wraps the whole chip when any inner control receives keyboard focus.
- disabled sets aria-disabled and disables the body and remove buttons.

## Do / Don't

| Do | Don't |
| --- | --- |
| Use chips for filters, selections, and removable tokens. | Use a chip for a primary page action — use Button. |
| Provide onRemove for dismissable input chips. | Nest a chip's remove action inside another clickable region. |
| Pair a type with a meaningful leading icon. | Rely on color alone to convey the chip's meaning. |
| Keep chip labels short and scannable. | Put long, wrapping sentences inside a chip. |

---

- Behaviour comes from a Radix primitive; cake& owns the visuals.
- Related: Badge, Button.
- Source: `src/cakeand/components/Chip/`
- Storybook: <https://cake.lenovo.com/storybook/?path=/docs/components-chip--docs>
