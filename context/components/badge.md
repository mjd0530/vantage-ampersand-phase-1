<!-- GENERATED by scripts/build-agent-context.mjs. Do not edit. -->
# Badge

```ts
import { Badge } from '@cake-admin/cakeand';
```

Badge is a compact, non-interactive status label (Figma Label, node 122:6268). Use it to tag or categorize content — a status, a count, a category — with an optional leading status dot. Reach for Chip when the pill needs to be selectable or removable, and Button for actions; Badge never responds to input.

## Props

| Prop | Type | Default | Notes |
| --- | --- | --- | --- |
| `children` | `React.ReactNode` | **required** | Badge text (Figma label). |
| `color` | `BadgeColor` | `'primary'` | Color family (Figma Property 1). Four semantic families — primary, secondary, destructive, disabled — draw from the design-system colors; the remaining values are decorative palette swatches. disabled is a muted/inactive treatment, not an interactive state (Badge is presentational). |
| `tone` | `'solid' \| 'subtle'` | `'solid'` | Emphasis (Figma Property 2). solid is a saturated fill with inverse text (Figma primary); subtle is a light tinted wash with colored text (Figma onPrimary). |
| `dot` | `boolean` | `true` | Show the leading 8px status dot (Figma showLeftSlot). The dot is decorative and inherits the badge's text color. |

Also accepts all `React.HTMLAttributes<HTMLSpanElement>` props.

## Usage

```tsx
<Badge>Label</Badge>
<Badge color="destructive">Error</Badge>
<Badge color="green" tone="subtle">Active</Badge>
<Badge color="blue">New</Badge>
<Badge color="disabled">Archived</Badge>
<Badge dot={false}>No dot</Badge>
<Badge role="status" color="green" tone="subtle">Saved</Badge>
```

## Design tokens used

`--radius-1000`, `--space-050`, `--space-100`, `--type-size-caption`, `--font-weight-bold`, `--color-primary-primary`, `--color-secondary-secondary`, `--color-error-error`, `--color-disabled-disabled-inverse`, `--color-text-icon-inverse`, `--color-disabled-disabled`, `--color-primary-primary-overlay`, `--color-secondary-secondary-overlay`, `--color-error-error-overlay`, `--color-badge-`, `--color-badge-text-icon-on-`

## Accessibility

- The badge is presentational; the leading dot is aria-hidden, so screen readers announce only the label text — never rely on the dot or color alone to convey meaning.
- color="disabled" is a muted visual treatment, not an interactive state — Badge sets no aria-disabled because there is nothing to disable.
- For status that updates live, add role="status" (or aria-live) so assistive tech announces the change.

## Do / Don't

| Do | Don't |
| --- | --- |
| Use Badge for static status, counts, and category tags. | Use a Badge for a clickable or removable pill — use Chip. |
| Keep the label short and scannable. | Put a wrapping sentence inside a badge. |
| Pair a color with clear label text. | Rely on color or the dot alone to convey meaning. |
| Use subtle on dense surfaces and solid for high emphasis. | Mix many palette colors without a consistent meaning. |

---

- Presentational; wraps no Radix primitive.
- Related: Button, Chip.
- Source: `src/cakeand/components/Badge/`
- Storybook: <https://cake.lenovo.com/storybook/?path=/docs/components-badge--docs>
