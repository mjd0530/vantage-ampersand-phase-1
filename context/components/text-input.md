<!-- GENERATED by scripts/build-agent-context.mjs. Do not edit. -->
# TextInput

```ts
import { TextInput } from '@cake-admin/cakeand';
```

A single-line text field, composed from the reusable form elements: Input Label above, the input box, and Helper String below — stacked with a --space-100 label gap and a --space-050 helper gap per the Figma auto-layout. Use Textarea (future) for multi-line text.

## Props

| Prop | Type | Default | Notes |
| --- | --- | --- | --- |
| `label` | `string` | — | Label text above the field (renders an InputLabel, wired via htmlFor). |
| `showLabelInfo` | `boolean` | `false` | Shows the label's 16px info icon. |
| `required` | `boolean` | `false` | Marks the field required: appends the label's "(required)" suffix and sets the native required attribute. |
| `helperText` | `React.ReactNode` | — | Helper text below the field (renders a HelperString, wired via aria-describedby). |
| `status` | `'default' \| 'success' \| 'error'` | `'default'` | Validation state: tints the field and the helper with the success/error tokens and sets aria-invalid on error. |
| `size` | `'sm' \| 'md'` | `'md'` | Field height: md 40px (24px slot icons), sm 32px (18px icons). |
| `startIcon` | `React.ReactNode` | — | Leading icon/content inside the field (inherits --color-text-icon-secondary). |
| `endIcon` | `React.ReactNode` | — | Trailing icon/content inside the field. |
| `maxLength` | `number` | — | Character limit — sets the native maxLength and shows the helper counter. |

Also accepts all `Omit<React.InputHTMLAttributes<HTMLInputElement>, 'size'>` props.

## Usage

```tsx
<TextInput label="Email" placeholder="you@lenovo.com" />
<TextInput label="Email" required helperText="Use your work email" />
<TextInput label="Search" startIcon={<Search />} endIcon={<X />} />
<TextInput label="Bio" maxLength={250} helperText="Keep it short" />
<TextInput label="Email" status="error" helperText="This field is required" />
<TextInput label="Email" size="sm" />
<TextInput label="Email" disabled />
```

## Design tokens used

`--color-surfaces-on-container-high`, `--stroke-100`, `--color-stroke-border`, `--color-stroke-border-high`, `--color-surfaces-container`, `--stroke-150`, `--color-primary-primary`, `--color-success-success-overlay`, `--color-success-success`, `--color-error-error-overlay`, `--color-error-error`, `--color-disabled-disabled`, `--color-disabled-disabled-inverse`, `--radius-200`, `--space-200`, `--space-100`, `--color-text-icon-secondary`, `--type-size-body`, `--color-text-icon-placeholder`

## Accessibility

- The label is a real <label htmlFor> — clicking it focuses the input; a labelless field must get aria-label (passed through to the input).
- The helper is wired via aria-describedby, so guidance and errors are announced with the field; status="error" also sets aria-invalid.
- Focus is conveyed by the typing border (--stroke-150 primary) — restyled, never removed.
- required renders the visible "(required)" suffix *and* sets the native attribute.

## Do / Don't

| Do | Don't |
| --- | --- |
| Always provide a label (or aria-label). | Use placeholder text as the label. |
| Put errors in helperText with status="error". | Signal errors with color alone. |
| Use maxLength to get the counter for free. | Show a counter without enforcing a limit. |
| Use size="sm" in dense layouts only. | Mix field sizes within one form. |

---

- Behaviour comes from a Radix primitive; cake& owns the visuals.
- Source: `src/cakeand/components/TextInput/`
- Storybook: <https://cake.lenovo.com/storybook/?path=/docs/components-text-input--docs>
