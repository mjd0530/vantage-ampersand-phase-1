<!-- GENERATED by scripts/build-agent-context.mjs. Do not edit. -->
# cake& component index — @cake-admin/cakeand (4.2.2)

Every component below is imported from ONE path:

```ts
import { Button, Card, TextInput } from '@cake-admin/cakeand';
```

There are no deep paths. `@cake-admin/cakeand/Button` does not resolve.

Every visual value is a CSS custom property — see `cake-tokens.md`.
Never hardcode a color, spacing, radius, or type size.

Read `components/<slug>.md` before using a component for the first time.

## Actions

- **Button** — The cake& action trigger — pill-shaped and token-driven. → `components/button.md`
- **Chip** — Chip represents a compact, discrete piece of information — a filter, a selection, or a re… → `components/chip.md`
- **IconButton** — A circular, icon-only action for toolbars, table rows, and other dense UIs where a text l… → `components/icon-button.md`

## Forms

- **Checkbox** — An independent on/off control — each checkbox stands alone, so any number can be checked… → `components/checkbox.md`
- **DateInput** — Date Input collects a typed MM/DD/YY date and exposes a calendar action that opens the br… → `components/date-input.md`
- **Dropdown** *(portals to body)* — A single-select field, composed from the reusable form elements: Input Label above, the s… → `components/dropdown.md`
- **FileUpload** — File Upload is an image-upload dropzone with a primary browse action. → `components/file-upload.md`
- **NumberDropdown** *(portals to body)* — A compact, numeric-only select for small closed ranges — page size, quantity caps, pagina… → `components/number-dropdown.md`
- **NumberInput** — A numeric stepper field, composed from the reusable form elements: Input Label above and… → `components/number-input.md`
- **PasswordInput** — Password Input is a password-creation field composed from Input Label, Radix's accessible… → `components/password-input.md`
- **PinInput** — A masked, multi-digit PIN or one-time-code field. → `components/pin-input.md`
- **RadioGroup** — A single-select control set — the user picks exactly one option from a short, mutually-ex… → `components/radio-group.md`
- **RangeSlider** — Range Slider selects a lower and upper bound along one track. → `components/range-slider.md`
- **Slider** — Slider selects one value along a bounded track. → `components/slider.md`
- **Switch** — A binary on/off control built on the Radix Switch primitive and styled with cake& tokens. → `components/switch.md`
- **TextInput** — A single-line text field, composed from the reusable form elements: Input Label above, th… → `components/text-input.md`
- **TimeInput** — Time Input collects a 12-hour time through separate hours and minutes segments plus an AM… → `components/time-input.md`

## Surfaces

- **Card** — An elevated surface that frames a slot of content. → `components/card.md`
- **ContentCard** — The horizontal content card template. → `components/content-card.md`
- **HeroCard** — The marketing hero card template. → `components/hero-card.md`
- **Modal** *(portals to body)* — Modal is the elevated dialog surface that combines the Modal elements into a complete, ac… → `components/modal.md`
- **ModalContent** — Modal Content is the body region of a modal dialog. → `components/modal-content.md`
- **ModalFooter** — Modal Footer is the bottom action region of a modal dialog. → `components/modal-footer.md`
- **ModalIcon** — Modal Icon is the 32px leading visual for modal headers. → `components/modal-icon.md`
- **ModalTitle** — Modal Title is the header region of a modal dialog. → `components/modal-title.md`
- **RichTooltip** *(portals to body)* — Rich Tooltip provides structured, contextual guidance with an optional title, step indica… → `components/rich-tooltip.md`
- **SimpleCard** — The vertical media card template. → `components/simple-card.md`
- **SimpleTooltip** *(portals to body)* — Simple Tooltip provides a short, supplemental text description. → `components/simple-tooltip.md`

## Navigation

- **Accordion** — A stack of collapsible sections. → `components/accordion.md`
- **Breadcrumb** *(portals to body)* — The trail showing where the current page sits in the hierarchy. → `components/breadcrumb.md`
- **ContentSwitcher** — A segmented control for switching between a small set of mutually exclusive views — a fil… → `components/content-switcher.md`
- **HorizontalTabItem** — One tab in a horizontal tab bar. → `components/horizontal-tab-item.md`
- **HorizontalTabs** — A horizontal tab bar and its panels. → `components/horizontal-tabs.md`
- **MenuContainer** — Menu Container is the elevated surface that groups menu content. → `components/menu-container.md`
- **MenuHeader** — Menu Header labels a group of related menu actions. → `components/menu-header.md`
- **MenuItem** — Menu Item is a compact action row for application menus. → `components/menu-item.md`
- **Sidebar** — The container for a sidebar rail and its panels. → `components/sidebar.md`
- **SidebarBlock** — A grouped sidebar section: a parent Sidebar Item sitting full-bleed across the top of a t… → `components/sidebar-block.md`
- **SidebarItem** — A top-level row in an app sidebar — taller than a vertical tab row, with a leading icon a… → `components/sidebar-item.md`
- **SidebarNav** — The app sidebar shell: a brand header, the navigation rail, and a footer holding the coll… → `components/sidebar-nav.md`
- **SidebarSubItem** — A nested row beneath a Sidebar Item — the second level of an app sidebar. → `components/sidebar-sub-item.md`
- **VerticalTabItem** — A top-level row in a vertical tab rail — the settings-style navigation where the rail sit… → `components/vertical-tab-item.md`
- **VerticalTabs** — The container for a vertical tab rail and its panels — the settings-style navigation wher… → `components/vertical-tabs.md`
- **VerticalTabSubItem** — A nested row beneath a Vertical Tab Item — the second level of a vertical rail, e.g. → `components/vertical-tab-sub-item.md`

## Data

- **ActionBar** — The toolbar above a table body. → `components/action-bar.md`
- **Avatar** — The circular representation of a person. → `components/avatar.md`
- **Badge** — Badge is a compact, non-interactive status label. → `components/badge.md`
- **Counter** — Counter is a compact numeric badge for notification counts — unread messages, pending ite… → `components/counter.md`
- **DataRow** — A single record in a table body. → `components/data-row.md`
- **HeaderRow** — The column-label row for a table. → `components/header-row.md`
- **Pagination** *(portals to body)* — Page navigation for a table or list. → `components/pagination.md`
- **RowsPerPage** — The page-size control that sits alongside Pagination: a "Rows per page:" label, a numeric… → `components/rows-per-page.md`
- **Table** — The assembled data table. → `components/table.md`

## Feedback

- **Notification** — A persistent notification-center card. → `components/notification.md`
- **NotificationPanel** — The bell/inbox panel that holds a list of notifications — the surface behind a header's n… → `components/notification-panel.md`
- **ProgressBar** — Progress Bar communicates a determinate amount of completed work. → `components/progress-bar.md`
- **Spinner** — Spinner communicates that work is in progress when no meaningful completion amount is ava… → `components/spinner.md`
- **Toast** — Toast is a transient notification surface, covering both Figma layouts: &Toast Simple (no… → `components/toast.md`

## Elements

- **Divider** — Divider separates adjacent groups of related content without adding a new heading or inte… → `components/divider.md`
- **HelperString** — The assistive line that sits under a form field — guidance, validation feedback, and an o… → `components/helper-string.md`
- **InputLabel** — The label row that sits above a form field — a reusable form element, not a standalone pa… → `components/input-label.md`
- **PasswordMenu** — Password Menu is static password-strength and policy feedback for an owning password field. → `components/password-menu.md`
- **Scrollbar** — The cake& scroll surface. → `components/scrollbar.md`

## Portalled components

Modal, Dropdown, SimpleTooltip, RichTooltip, Breadcrumb, NumberDropdown, Pagination render into `document.body`, outside the React tree. They stay themed only because `CakeProvider` puts `data-theme` on `<html>`. Do not pass `scope="subtree"` if you use any of them.

## Rules

1. Exactly one `<CakeProvider>`, at the app root.
2. Never hardcode a color, spacing, radius, or type size — use tokens.
3. Token names are exact, not guessable by analogy. Check `cake-tokens.md`.
4. `styled-components` must resolve to a single instance.
5. Icons come from `lucide-react`.
6. Do not add another UI library. Compose from what is here.

63 components. Live docs: <https://cake.lenovo.com/storybook/>
